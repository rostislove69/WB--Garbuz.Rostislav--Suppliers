import React from "react";
import MainHeader from "../MainHeader/MainHeader";
import Table from "../Table/Table";

const Main: React.FC = () => {
  return (
    <main>
      <MainHeader />
      <Table />
    </main>
  );
};

export default Main;
